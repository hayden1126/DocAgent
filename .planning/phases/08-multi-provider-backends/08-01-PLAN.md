---
phase: 08-multi-provider-backends
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docagent/backends/litellm_backend.py
  - pyproject.toml
  - scripts/measure_citation_rate.py
autonomous: true
requirements: [BACKEND-01]
must_haves:
  truths:
    - "`docagent/backends/litellm_backend.py` exists on main with the prototype loop, tool dispatch, and `_safe_path` from the spike branch."
    - "`from docagent.backends.litellm_backend import LiteLLMBackend, BackendUnavailableError` succeeds without `litellm` installed (lazy import inside `run()`)."
    - "Importing `LiteLLMBackend` does NOT print the Bedrock/SageMaker pre-load WARN lines from LiteLLM, even when `litellm` IS installed."
    - "`pyproject.toml` declares the `[multi]` extra pinning `litellm>=1.50` and bumps `pydantic` floor to `>=2.10` to match LiteLLM's floor."
    - "`pyproject.toml` carries a mypy override allowing `litellm.*` to be untyped (so default-extras users with no `litellm` installed don't break `mypy docagent/`)."
    - "`scripts/measure_citation_rate.py` exists (renamed from `scripts/spike_phase8_citation_rate.py`) and is importable/runnable."
    - "Existing `AgentSDKBackend` tests still pass (385 tests at phase start; no regression on the SDK path)."
  artifacts:
    - path: "docagent/backends/litellm_backend.py"
      provides: "LiteLLMBackend dataclass + tool dispatch + _safe_path + BackendUnavailableError + LiteLLM logger silencer"
      contains: "class LiteLLMBackend"
      min_lines: 220
    - path: "pyproject.toml"
      provides: "[multi] extras, pydantic>=2.10 floor, litellm.* mypy override"
      contains: "multi = ["
    - path: "scripts/measure_citation_rate.py"
      provides: "Permanent citation-rate measurement tool (renamed from scripts/spike_phase8_citation_rate.py)"
      contains: "citation"
  key_links:
    - from: "docagent/backends/litellm_backend.py::LiteLLMBackend.run"
      to: "litellm.completion(...)"
      via: "lazy import inside run() — never at module top"
      pattern: "import litellm"
    - from: "docagent/backends/litellm_backend.py (module load)"
      to: "logging.getLogger('LiteLLM').setLevel(logging.ERROR)"
      via: "import-time silencer set BEFORE any `import litellm`"
      pattern: "getLogger\\(\"LiteLLM\"\\).*ERROR"
    - from: "pyproject.toml::project.optional-dependencies"
      to: "litellm>=1.50"
      via: "[multi] extras"
      pattern: "multi\\s*="
---

<objective>
Port the spike-branch prototype (`spike/phase-8-litellm` commits
`e945838` + `5179d4d`) onto `main` with three small fixes folded in:
(a) silence LiteLLM's Bedrock/SageMaker pre-load WARN lines that fire
on `import litellm`; (b) bump DocAgent's `pydantic` floor from `>=2.7`
to `>=2.10` to match LiteLLM 1.85.0's floor (Pitfall 5 in RESEARCH.md);
(c) rename the spike script to a permanent location.

Purpose: Establishes the file landing surface for Plans 08-02 through
08-06. No CLI wiring yet, no pricing shim yet, no allowlist gate yet —
those are sequenced into later waves so a single PLAN doesn't try to
do everything. The spike code is already ruff + mypy-strict clean
against the spike pyproject override (RESEARCH.md confirms).

Output: `LiteLLMBackend` importable from `docagent.backends.litellm_backend`
on main; `pip install -e ".[multi]"` works; default `pip install -e .`
does NOT pull `litellm` (small dep tree preserved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-multi-provider-backends/08-CONTEXT.md
@.planning/phases/08-multi-provider-backends/RESEARCH.md
@.planning/decisions/0001-phase-8-multi-provider-backend.md
@.planning/decisions/0001-spike-results.md
@docagent/backends/base.py
@docagent/backends/agent_sdk.py
@pyproject.toml

<interfaces>
<!-- The protocol surface for backends. Spike LiteLLMBackend ALREADY -->
<!-- conforms; this plan only ports + polishes, doesn't redesign.    -->

From docagent/backends/base.py:
- @dataclass(frozen=True, slots=True) class GenerationRequest:
      artifact_id: str
      prompt: str
      repo_root: Path
      max_iterations: int = 24
      extras: dict[str, str] | None = None
- @dataclass(frozen=True, slots=True) class GenerationResponse:
      content: str
      tool_calls: int = 0
      input_tokens: int = 0
      output_tokens: int = 0
- @runtime_checkable class LLMBackend(Protocol):
      name: str
      def run(self, request: GenerationRequest) -> GenerationResponse: ...

From docagent/backends/agent_sdk.py (sibling backend, the pattern to mirror):
- `_SYSTEM_PROMPT` constant block (lines 43-52) — DocAgent grounding/citation rules.
- `class BackendUnavailableError(RuntimeError)` — install-hint exception.
- Lazy import pattern: `from claude_agent_sdk import ...` is inside run(), not at module top.
- `name: str = "agent_sdk"` class attribute.

Spike file to port (verbatim except the polish below):
- `spike/phase-8-litellm:docagent/backends/litellm_backend.py` (~270 LOC).
- Already ruff + mypy strict-clean against the spike pyproject override.
- `_SYSTEM_PROMPT` (lines 27-38) — SAME content as agent_sdk's prompt (CONTEXT.md locks "single prompt for all providers"). Do NOT modify; do NOT bump PROMPT_VERSION (that's for artifact prompts, not backend prompts).

Polish to apply during the port (the only deltas from spike contents):
1. INSERT before the `import litellm` line inside `run()`:
       import logging as _logging_mod
       _logging_mod.getLogger("LiteLLM").setLevel(_logging_mod.ERROR)
   This silences the Bedrock/SageMaker pre-load WARNs (Pitfall 6).
2. Wrap the FIRST `completion(...)` call in a try/except for
   `litellm.AuthenticationError`, re-raising as `BackendUnavailableError`
   with a multi-provider hint (RESEARCH.md "Environment Availability" snippet).
3. Defensive guard: if `not response.choices: continue` before reading
   `response.choices[0]` (Pattern 1 gap #7).
4. Defensive guard: when looping `for _turn in range(self.max_turns)`,
   if the loop exits without breaking (i.e. the model never stopped
   calling tools), emit `_log.warning("litellm_backend %s: max_turns=%d exhausted", request.artifact_id, self.max_turns)` after the loop. Pattern 1 gap #2.
5. No other source changes. Specifically: do NOT add the allowlist gate,
   do NOT add the pricing shim, do NOT add a `_warn_unsupported_model`
   helper — those land in Plans 08-02 and 08-03.

From spike `pyproject.toml` (already prepared on spike branch):
- `[project.optional-dependencies]` has `multi = ["litellm>=1.50"]` block.
- `[[tool.mypy.overrides]]` has `module = ["litellm", "litellm.*"]` with
  `ignore_missing_imports = true` and `disallow_untyped_defs = false`.

Delta to apply to main's pyproject.toml:
- Copy the `[multi]` extras block from spike branch verbatim.
- Copy the `[[tool.mypy.overrides]]` block for `litellm.*` from spike.
- Bump `pydantic>=2.7` → `pydantic>=2.10` (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port spike LiteLLMBackend to main with logger silencer + auth wrap + defensive guards</name>
  <files>docagent/backends/litellm_backend.py</files>
  <action>Bring the spike file over verbatim, then apply ONLY the five polish deltas listed in `<interfaces>` above. Concretely:
  (1) Run `git checkout spike/phase-8-litellm -- docagent/backends/litellm_backend.py` from the repo root to land the spike file.
  (2) Open the new file and EDIT inside `LiteLLMBackend.run()`: immediately before the line `import litellm`, insert two lines (sharing the existing leading indentation):
        import logging as _logging_mod
        _logging_mod.getLogger("LiteLLM").setLevel(_logging_mod.ERROR)
  This is the LiteLLM Bedrock/SageMaker pre-load WARN silencer (Pitfall 6).
  (3) Right after `litellm.drop_params = True`, wrap NOTHING yet — the AuthenticationError wrap goes around the first `completion(...)` call. Wrap the existing `response = completion(...)` line (inside the `for _turn in range(self.max_turns)` loop) on the FIRST iteration only by introducing a small helper, or simpler: keep the loop body but add a `try/except litellm.AuthenticationError` AROUND the `response = completion(...)` call, re-raising as `BackendUnavailableError("LiteLLM authentication failed for model {self.model!r}. Ensure the appropriate API key env var is set (GEMINI_API_KEY, OPENROUTER_API_KEY, or ANTHROPIC_API_KEY).") from exc`. This means importing `BackendUnavailableError` at the top of the call (it's defined in the same module) — no extra import needed. Verbatim hint string per RESEARCH.md "Environment Availability".
  (4) Add a defensive guard: immediately after `response = completion(...)` (and the AuthenticationError wrap), insert `if not response.choices: continue`. Place BEFORE the `choice = response.choices[0]` line.
  (5) After the `for _turn in range(self.max_turns):` loop body, add an `else:` clause that fires only if the loop exhausted without `break`:
        else:
            _log.warning(
                "litellm_backend %s: max_turns=%d exhausted without terminating turn",
                request.artifact_id, self.max_turns,
            )
  This pins Pattern 1 gap #2.
  Do NOT touch `_SYSTEM_PROMPT`, `_TOOLS_SPEC`, `_safe_path`, `_read`, `_glob`, `_grep`, or any other helper. Do NOT add `_TESTED_MODELS`, do NOT import the pricing shim, do NOT add a `_warn_unsupported_model` helper — those land in plans 08-02 and 08-03.</action>
  <verify>
    <automated>python -c "from docagent.backends.litellm_backend import LiteLLMBackend, BackendUnavailableError; b = LiteLLMBackend(model='anthropic/claude-sonnet-4-6'); print(b.name, b.model, b.max_turns)" 2>&1 | tail -5</automated>
  </verify>
  <done>`docagent/backends/litellm_backend.py` exists on main, imports without `litellm` being installed (lazy import inside `run()` only), `LiteLLMBackend()` instantiates with the default model, `grep -n 'getLogger(\"LiteLLM\")' docagent/backends/litellm_backend.py | grep -v '^#'` returns ≥1 hit, `grep -n 'AuthenticationError' docagent/backends/litellm_backend.py | grep -v '^#'` returns ≥1 hit, `grep -n 'max_turns=%d exhausted' docagent/backends/litellm_backend.py | grep -v '^#'` returns 1 hit, `grep -n 'not response.choices' docagent/backends/litellm_backend.py | grep -v '^#'` returns 1 hit. No `_TESTED_MODELS` constant present yet (verify via `! grep -q '_TESTED_MODELS' docagent/backends/litellm_backend.py`).</done>
</task>

<task type="auto">
  <name>Task 2: Pyproject — [multi] extras + pydantic floor bump + litellm mypy override</name>
  <files>pyproject.toml</files>
  <action>Edit `pyproject.toml` with three minimal deltas. (1) Bump the `pydantic>=2.7` pin on line ~36 to `pydantic>=2.10` (Pitfall 5 — LiteLLM 1.85.0 requires `pydantic >=2.10.0,<3.0.0`; aligning DocAgent's documented floor eliminates resolver surprise). (2) Add a `[project.optional-dependencies]` `multi` block — if the `[project.optional-dependencies]` table already exists (it does on main with `dev = [...]`), append a `multi = ["litellm>=1.50"]` line under it; do NOT clobber the `dev` entry. (3) Append a `[[tool.mypy.overrides]]` block at the end of the existing mypy section setting `module = ["litellm", "litellm.*"]`, `ignore_missing_imports = true`, `disallow_untyped_defs = false`. Mirror the exact form on `spike/phase-8-litellm:pyproject.toml` lines 80-83 — verify by running `git diff spike/phase-8-litellm -- pyproject.toml` and confirming the only remaining diffs after your edit are the spike's mypy override block AND the `multi` extras block both being present (i.e. main now matches spike on those blocks). After editing, run `pip install -e ".[dev]"` to confirm the default-extras path still resolves (no LiteLLM pulled). Then run `pip install -e ".[multi]"` and verify `litellm==1.x.y` appears in `pip list | grep litellm`. Roll back the venv if a co-install conflict surfaces — RESEARCH.md's "Empirical co-install verdict" says it won't, but verify in the actual project venv.</action>
  <verify>
    <automated>pip install -e ".[multi]" 2>&1 | tail -5 && pip list 2>/dev/null | grep -iE 'litellm|pydantic|claude-agent-sdk' | head</automated>
  </verify>
  <done>`grep -n 'pydantic>=' pyproject.toml` shows `pydantic>=2.10`. `grep -n 'multi = ' pyproject.toml` matches `multi = ["litellm>=1.50"]`. `grep -B1 -A2 'litellm' pyproject.toml | grep -E 'module|ignore_missing_imports'` shows the mypy override is wired. `pip install -e ".[multi]"` exits 0 and `pip list | grep -i litellm` shows litellm installed. Existing tests still pass: `pytest tests/unit/test_backend_token_extraction.py tests/unit/test_pricing.py tests/unit/test_budget.py -x 2>&1 | tail -3` returns 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Rename spike script to a permanent location</name>
  <files>scripts/measure_citation_rate.py</files>
  <action>Use `git mv` to rename `scripts/spike_phase8_citation_rate.py` → `scripts/measure_citation_rate.py` AFTER landing it from the spike branch:
  (1) `git checkout spike/phase-8-litellm -- scripts/spike_phase8_citation_rate.py` from the repo root.
  (2) `git mv scripts/spike_phase8_citation_rate.py scripts/measure_citation_rate.py`.
  (3) Edit `scripts/measure_citation_rate.py`: update the module-docstring (top of file) to drop any "spike" branch-name leak. Replace the docstring's opening line with `"""Citation-rate measurement tool for LLM backends."""` and update any internal references to `spike` in comments to refer to "measurement runs" instead. If there's a `__main__` block with `argparse`, ensure the program description doesn't say "spike". Do NOT change the actual measurement logic — it's a working tool we want available for future allowlist additions (Ollama re-spike, new Gemini SKUs, etc.).
  (4) Verify the script's `--help` text is sane: `python scripts/measure_citation_rate.py --help 2>&1 | head` should print without traceback. If the script imports `LiteLLMBackend`, it will work because Task 1 landed that file. If the script imports from a path that doesn't exist on main (e.g. spike-only fixture), surface that in the SUMMARY but don't block — the script is a future-spike tool, not a CI gate.</action>
  <verify>
    <automated>test -f scripts/measure_citation_rate.py && ! test -f scripts/spike_phase8_citation_rate.py && python scripts/measure_citation_rate.py --help 2>&1 | head -3</automated>
  </verify>
  <done>`scripts/measure_citation_rate.py` exists; `scripts/spike_phase8_citation_rate.py` does NOT exist; the script runs `--help` without traceback; `grep -c 'spike' scripts/measure_citation_rate.py` returns 0 in the docstring/program-description area (occasional `spike` references in inline comments are OK if they reference the measurement methodology rather than the branch name).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip resolver → user venv | New `[multi]` extras pulls `litellm` and its 25-40 transitive deps |
| LiteLLM logger → stderr | LiteLLM emits Bedrock/SageMaker pre-load WARNs at import time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `litellm` dependency tree (25-40 transitive packages) | mitigate | RESEARCH.md "Package Legitimacy Audit" confirms litellm provenance (BerriAI/litellm, >25k commits, daily releases, ~10M+/month downloads). Re-verify via `slopcheck install litellm` before merging if `slopcheck` CLI is available; otherwise rely on RESEARCH.md audit. `[CITED: github.com/BerriAI/litellm + empirical install 2026-05-17]` |
| T-08-02 | Information Disclosure | Bedrock/SageMaker pre-load WARN to stderr | accept | Cosmetic — no PII or secret in the WARN. Silenced via `logging.getLogger("LiteLLM").setLevel(logging.ERROR)` at module load to keep `docagent init` output clean. |
| T-08-SC | Tampering | npm/pip/cargo installs (this plan's `pip install -e ".[multi]"`) | mitigate | Single new package (`litellm`); RESEARCH.md Package Legitimacy Audit marks it `[CITED]` with three independent provenance signals. No `[ASSUMED]` or `[SUS]` packages in this extras block. |
</threat_model>

<verification>
- `python -c "from docagent.backends.litellm_backend import LiteLLMBackend"` succeeds on a venv WITHOUT `[multi]` installed (lazy import inside `run()` proves the module top is import-safe).
- `pip install -e ".[multi]"` exits 0; `pip list | grep litellm` shows litellm 1.x.y.
- `ruff check docagent/backends/litellm_backend.py` clean.
- `mypy docagent/backends/litellm_backend.py` clean (litellm mypy override means missing-stubs is tolerated).
- `pytest tests/unit/ -x 2>&1 | tail -3` returns 0 failures (existing 385+ tests still green).
- `git diff spike/phase-8-litellm main -- docagent/backends/litellm_backend.py` shows ONLY the five intentional polish deltas (logger silencer, AuthenticationError wrap, `not response.choices` guard, `else:` warn on max_turns exhaustion). No drift in `_SYSTEM_PROMPT`, `_TOOLS_SPEC`, or tool helpers.
</verification>

<success_criteria>
- `LiteLLMBackend` ported from spike with five polish deltas applied (no other source changes).
- `pip install docagent` (no extras) does NOT pull `litellm`.
- `pip install docagent[multi]` pulls `litellm>=1.50` cleanly alongside existing deps.
- `pydantic>=2.10` is the new effective floor in `pyproject.toml`.
- LiteLLM's Bedrock/SageMaker pre-load WARNs are silenced for users running `docagent init --backend litellm` (verified by manual stderr capture in a follow-up plan).
- `scripts/measure_citation_rate.py` exists; `scripts/spike_phase8_citation_rate.py` removed.
- Existing test suite still green.
</success_criteria>

<output>
Create `.planning/phases/08-multi-provider-backends/08-01-SUMMARY.md` when done.
</output>
